# Backend ortam değişkenleri — kopyala: cp .env.example .env — sonra değerleri doldur.
# .env dosyası git'e GİRMEZ (.gitignore). Gerçek key'leri sadece .env içinde tut.

PORT=8080
HOST=0.0.0.0
NODE_ENV=development
# Supabase PostgreSQL (Dashboard → Settings → Database → Connection string → URI)
# Yerel: aynı URL veya kendi Postgres. Railway: bu değişkeni mutlaka ekleyin.
# Pooler (pooler.supabase.com:6543): Backend otomatik pgbouncer=true, connection_limit, connect_timeout=15, pool_timeout=20 ekler.
# connection_limit: Production varsayılan 5, development 3; 1–10 arası (DATABASE_POOL_SIZE ile). URL'de connection_limit=1 kullanmayın.
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.XXXX.supabase.co:5432/postgres"
# DATABASE_POOL_SIZE=5

# Production: Mutlaka güçlü ve benzersiz. Örn: openssl rand -base64 32
JWT_SECRET=<güçlü-rastgele-string-en-az-32-karakter>
JWT_EXPIRES_IN=7d

SUPABASE_URL=https://xxx.supabase.co
SUPABASE_ANON_KEY=<anon_key>
SUPABASE_SERVICE_ROLE_KEY=<service_role_key_Dashboard_Settings_API>

# Admin paneli + mobil Admin butonu: Bu id'ye sahip kullanıcı admin sayılır (Prisma Kullanici.id = CUID string, örn. clxx...)
# ADMIN_KULLANICI_ID=clxxxxxxxxxxxxxxxxxxxxxxxx
# KBS: Jandarma/Polis web servis URL'leri. Gerçek KBS adresini yazın; boş bırakırsanız "JANDARMA_KBS_URL tanımlı değil" hatası alırsınız (example.com kullanılmaz).
# Bağlantı: tesis kodu + web servis şifresi (TEXT; özel karakterler $ ? vb. JSON body'de güvenle gider) + sunucu IP whitelist (docs/KBS_BAGLANTI_GEREKSINIMLERI.md).
# VPS: GET /debug/egress-ip ile çıkan IP'yi KBS'ye beyan edin.
# Jandarma KBS (SOAP). Deploy'da mutlaka set edin; boşsa mock kullanılır.
JANDARMA_KBS_URL=https://vatandas.jandarma.gov.tr/KBS_Tesis_Servis/SrvShsYtkTml.svc
# POLIS_KBS_URL=https://...
SYNC_BRANCH_SECRET=<sync_secret>
# Production: Admin panel NEXT_PUBLIC_ADMIN_SECRET ile aynı olmalı. Güçlü ve benzersiz.
ADMIN_SECRET=<admin_secret>

# Production (opsiyonel): Sadece panel domain. Birden fazla: virgülle (https://panel1.com,https://panel2.com)
# CORS_ORIGIN=https://panel-alanadiniz.com

# MRZ hata ayıklama: true veya 1 ise OCR metni, extract sonucu ve parse adımları konsola loglanır (Railway loglarında izlenebilir).
# DEBUG_MRZ=true
# Ön işlenmiş MRZ görüntülerini backend/debug-images klasörüne kaydetmek için (kağıt/fotokopi pipeline test).
# SAVE_DEBUG_IMAGES=true

# Railway deploy için (opsiyonel)
# RAILWAY_TOKEN=<railway_project_token>
