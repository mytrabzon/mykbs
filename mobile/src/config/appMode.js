/**
 * MRZ ekranında sadece pasaport (TC Kimlik kartı seçeneği kapalı).
 * Uygulama tam özellikli kalır: Odalar, Misafirler, Raporlar, Drawer menü vb. hepsi açık.
 */
export const MRZ_PASSPORT_ONLY = true;
