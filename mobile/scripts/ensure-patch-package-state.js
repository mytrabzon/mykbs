// Optional: add pre-created .patch-package.json state for any package that
// is patched but not installed (e.g. optional deps). Currently all patches
// target installed packages (expo-modules-core, react-native-iap).
// No-op for this project.
